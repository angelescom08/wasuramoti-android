<manifest 
android:versionName="0.5.3" android:versionCode="8" package="karuta.hpnpwd.wasuramoti" xmlns:android="http://schemas.android.com/apk/res/android">
<uses-sdk android:minSdkVersion="7"></uses-sdk>
    <application android:icon="@drawable/animals_dolphin" android:label="@string/app_name">
        <receiver android:name=".NotifyTimerReceiver" />
        <activity android:label="@string/app_name" android:name=".WasuramotiActivity" android:configChanges="orientation|keyboardHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".ConfActivity" android:configChanges="orientation|keyboardHidden"/>
        <activity android:name=".NotifyTimerActivity" android:configChanges="orientation|keyboardHidden"/>
    </application>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
